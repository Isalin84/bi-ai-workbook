# Рабочая тетрадь «Нейросети для Boehringer Ingelheim»

Интерактивная тетрадь практикума по генеративным нейросетям для сотрудников российского
представительства Boehringer Ingelheim. Ведёт Иван Салин, Best Practice.

- Тетрадь: `https://isalin84.github.io/bi-ai-workbook/`
- Табло ведущего: `https://isalin84.github.io/bi-ai-workbook/tablo.html`

Один файл `index.html`, без сборки и внешних библиотек. Открывается и с сайта, и с диска двойным кликом.
Все ответы и баллы участника живут в его браузере (localStorage), никуда не отправляются.

## Что в папке

| Путь | Что это |
|---|---|
| `index.html` | Тетрадь целиком: контент, движок баллов, звуки, диплом |
| `tablo.html` | Табло ведущего: вставить коды участников, построить рейтинг, показать победителей на проекторе |
| `assets/` | Логотипы Best Practice и Boehringer Ingelheim (в HTML они вшиты base64, файлы здесь для справки) |
| `assets/illustrations/` | Иллюстрации блоков и диплома: `b0-start.webp … b7-final.webp, slovar.webp, diploma.webp`. Бумажная аппликация, сделаны в ChatGPT; промпты, исходники и скрипт обрезки лежат в `../_tools/`. Без файлов тетрадь тоже работает |
| `data/` | Учебные датасеты: продажи по категориям за 2025 год (CSV + XLSX, второй лист - справочник препаратов) и медиа-упоминания (CSV). Данные выдуманы, препараты, клиенты и фамилии синтетические |
| `examples/` | Эталонные мини-приложения: дашборд продаж и обезличиватель для фармы (текст + таблицы) |
| `print/` | Памятка по безопасности и каталог нейросетей на печать, А4 |
| `handbook/` | Учебный справочник «Здоровье в офисе» (MD, HTML, PDF) - документ для проекта в блоке 6. Лежит в тетради, чтобы не зависеть от чужих сайтов |
| `docs/` | Локальная копия Кодекса поведения BI, осталась с первой версии блока 6. В репозиторий не попадает (`.gitignore`) |

Генераторы лежат вне репозитория, в `../_tools/`: `gen_sales_data.py` (продажи, seed фиксирован), `gen_pharma_data.py` (медиа-файл; он же когда-то делал реестр обучений, который из тетради убран),
`gen_catalog_print.js` (каталог на печать собирается из массива `CATALOG` в `index.html` - руками не править,
запускать `node _tools/gen_catalog_print.js`). Иллюстрации: `illustration_prompts.md` (промпты), `illustrations_src/` (PNG из ChatGPT),
`crop_illustrations.py` (обрезка и webp в `assets/illustrations/`).

## Сценарий дня, 13:00–18:00

| Блок | Время | Баллы | Код ведущего |
|---|---|---|---|
| 0 · Вход и доступы | 13:00–13:15 | 120 | |
| ★ · Словарь | весь день | 60 | |
| 1 · Миф или правда | 13:15–13:30 | 50 | **ТОКЕН** - когда дойдёте до вопроса про русские токены |
| 2 · Безопасность и данные | 13:30–14:15 | 180 | **ГАЗЕТА** - после «теста газеты» |
| 3 · Промпт-инжиниринг | 14:15–15:00 | 160 | |
| перерыв | 15:00–15:15 | | |
| 4 · Данные и таблицы | 15:15–16:15 | 260 | **ДАШБОРД** - когда у первого участника открылся дашборд |
| 5 · Картинки, музыка и видео | 16:15–16:55 | 160 | **АНТОШКА** - когда зазвучит первый трек |
| перерыв | 16:55–17:05 | | |
| 6 · Ассистент в проекте Qwen | 17:05–17:35 | 60 | **ИНГЕЛЬХАЙМ** - когда ассистент первый раз сослался на раздел справочника |
| 7 · Финал, план, диплом | 17:35–18:00 | 50 | |

Базовых баллов в заданиях 1100, планка для уровней и шкалы - 1000, её можно перепрыгнуть. Уровни: Молекула (до 250) → Доклиника (250–500) →
Клинические исследования (500–750) → Регистрация (750 и выше).

Бонусы сверх тысячи: Leonardo +25, Suno +25, светофор без ошибок +40, первый дашборд +50,
три идеи приложений +25, своё приложение +35, все три Антошки +20, пять кодов ведущего по +30.

Блок 4 подробнее: две формулы (40), закономерности в продажах (60) и в медиа (30), дашборд (70), игра в Qwen (60).
Блок 5: кадр (30), оживить кадр в видео (30), четыре портрета по своему фото, любые два (40), Антошка (30), оживить портрет (30).

## Как проходит день

1. **До семинара.** Письмом за неделю: зарегистрироваться в Qwen, Kimi, Arena; по желанию Leonardo и Suno.
   Попросить ИТ клиента проверить из корпоративной сети: `isalin84.github.io`, `chat.qwen.ai`, `kimi.com`,
   `arena.ai`, `leonardo.ai`, `suno.com`, `fonts.googleapis.com`. Если github.io закрыт - тетрадь раздаётся
   папкой на флешке, работает с диска.
2. **Блок 0.** Участник заполняет профиль (имя, должность, отдел, «чем занимаюсь») - эти данные подставляются
   во все промпты и в диплом. Отмечает регистрации.
3. **По ходу дня** ведущий называет кодовые слова. Участник вводит их в «Справочник» (кнопка в шапке).
   Проверка терпима к регистру, Ё и раскладке клавиатуры: «byutkm[fqv» засчитается как ИНГЕЛЬХАЙМ.
4. **Блок 7.** Участник нажимает «Сформировать код» и отправляет строку в чат группы.
5. **Табло.** Ведущий вставляет всё содержимое чата в `tablo.html` (мусор отбрасывается сам, дубли схлопываются
   по большему баллу), нажимает «Показать на проекторе». Пробел, → , Enter или кликер - следующая стадия:
   3 место → 2 место → 1 место с фанфарами и конфетти → все участники. Esc - закрыть. CSV выгружается кнопкой.
6. **Диплом.** Собирается в блоке 7 в цветах Boehringer Ingelheim, скачивается PNG или печатается в PDF.

## Что спрятано в данных

**Продажи по категориям 2025** (371 отгрузка, 8 регионов, 5 каналов, 18 клиентов, 6 категорий, 11 вымышленных препаратов):
1. «Респираторные» - сезонность: с октября по февраль в среднем 4350 упаковок в месяц против 760 летом, разница в 5-6 раз.
2. Дистрибьютор «Фарма-Логистик» - средняя скидка 19 % против 8 % у всех остальных.
3. «Кардистан», май 2025 - возвраты 8 % упаковок против 0,1 % в любой другой месяц. Похоже на проблемную серию.

**Медиа-упоминания** (180 строк, март 2025 – февраль 2026, 10 вымышленных изданий):
1. Сентябрь 2025 - доля негатива 56 % против 11 % в остальные месяцы, тема «доступность и цены».
2. «Деловой курьер» - среднее время ответа 43 часа против 12 у остальных.
3. Запросы в выходные отвечают в 2,8 раза дольше, чем в будни.

Задание в тетради принимает ответ, если в нём есть слова-маркеры находок (см. `keys` в задачах `find_sales`
и `find_media`). Перед проверкой участник подтверждает, что удалил ФИО из файла; если в ответе нейросети
проскочила фамилия или телефон - проверка не пропустит.

## Как править

Контент живёт в массивах в начале `<script>` в `index.html`:
`CATALOG` (сервисы), `DOCS` (справочник для блока 6), `GLOSSARY` и `GLOSSARY_EXTRA` (словарь), `IMAGE_PROMPTS` (четыре
промпта для картинок, портреты с вкладками женский/мужской), `ANTOSHKA_LYRICS` и `MUSIC_GENRES` (Suno), `BLOCKS` (все блоки
и задания), `CODES` (кодовые слова), `LEVELS` (уровни). Дальше по файлу: `ROLES`/`DEPTS` (подсказки в профиле), `BF` (пример
формулы промпта), `PII` (что ловит проверка на персональные данные), `PALETTE` и `appPrompt()` (промпт дашборда),
`GAME_THEMES` и `gamePrompt()` (игра в Qwen), `FRAME` и `MOTION` (конструкторы кадра и видео), `SYS_BASE`/`CTRL_MAIN`/`CTRL_TRAP`/`CTRL_ROLE`
(ассистент по здоровью, системный промпт до 1000 знаков - столько принимает Qwen), `ASST_IDEAS` (пять сценариев
ассистентов с готовыми промптами). Поменяли текст в массиве - поменялась страница.

Сумма баллов заданий в блоке должна совпадать с `pts` блока. Планка `MAX` в ядре зафиксирована на 1000 и от суммы блоков не зависит. Проверка:

```bash
node -e "const s=require('fs').readFileSync('index.html','utf8');const a=s.indexOf('var CATALOG=['),b=s.indexOf('/* ================================ ЯДРО');const B=new Function(s.slice(a,b)+';return BLOCKS')();console.log(B.reduce((x,y)=>x+(y.pts||0),0));B.forEach(b=>console.log(b.id,b.pts,b.tasks.filter(t=>t.pts).reduce((x,t)=>x+t.pts,0)))"
```

## Технические заметки

- Звуки синтезируются через Web Audio, файлов нет. Кнопка «Звук» в шапке, состояние запоминается.
  Звук включается только после первого клика на странице - так требует браузер.
- Конфетти рисуется на canvas без библиотек; выключено для пользователей с «уменьшить движение».
- Диплом рисуется на canvas 1754×1240 после загрузки Montserrat (ждём до 1,5 с, потом рисуем системным шрифтом).
  `assets/illustrations/diploma.webp` встаёт в правую колонку; если файл убрать, там рисуются три пастельные плашки.
- Шрифты грузятся с Google Fonts; без сети подставляются системные, всё работает.
- Ключ localStorage тетради `bi_pharma_wb_v1`, табло `bi_tablo_v1` - не пересекаются со старой тетрадью солодовни.
- Справочник для блока 6 собирается из `handbook/*.md`: `pandoc` в HTML, обёртка со стилями, PDF через headless Chrome
  (`--print-to-pdf`). Правите Markdown - пересобираете обе версии.

## Публикация

```bash
git add -A && git commit -m "правка" && git push
```

GitHub Pages собирает сайт из корня ветки `main`, обновление приходит за минуту. `.nojekyll` отключает
обработку Jekyll. Папка `docs/` в репозиторий не попадает.

---
Best Practice · bestpracticeai.ru · Создано с Best Practice AI
